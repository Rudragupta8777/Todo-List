<resources>
    <!-- Base application theme -->
    <style name="Theme.TodoList" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Custom attributes -->
        <item name="backgroundColor">@color/light_background</item>
        <item name="textColor">@color/light_text</item>
        <item name="cardBackground">@color/light_card_background</item>
        <item name="checkboxTint">@color/light_checkbox_tint</item>
        <item name="progressBarColor">@color/light_progress_bar</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/backgroundColor</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.TodoList.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>
